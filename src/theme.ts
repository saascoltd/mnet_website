import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fab704", // golden yellow
    },
    secondary: {
      main: "#2b31a9", // deep blue
    },
  },
});

export default theme;
