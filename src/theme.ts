import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f6af05", // golden yellow
    },
    secondary: {
      main: "#262ca0", // deep blue
    },
  },
});

export default theme;
